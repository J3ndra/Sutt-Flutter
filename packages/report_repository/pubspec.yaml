name: report_repository
description: "Dart package for report repository"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.4.0
  cloud_firestore: ^4.17.0
  firebase_storage: ^11.7.2

  # User repository
  user_repository:
    path: ../user_repository
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true